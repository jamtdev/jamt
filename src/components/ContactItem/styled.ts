import styled from 'styled-components';
import { spacing } from 'src/config/theme';

export const Container = styled.div`
  display: flex;
  align-items: center;
  margin: ${spacing.BASE_3} 0;
`;

export const IconContainer = styled.div`
  display: flex;
  align-items: center;
  margin-right: ${spacing.BASE_3};
`;
